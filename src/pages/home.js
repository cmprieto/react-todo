import { Fragment } from "react";
import TodoContainer from "../components/TodoContainer";

const Home = () => {
  return (
    <Fragment>
      <TodoContainer />
    </Fragment>
  );
};

export default Home;
