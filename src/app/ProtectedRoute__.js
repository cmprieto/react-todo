/* import { Navigate } from 'react-router-dom';

const ProtectedRoute = ({ component }) => {
  const { isAuthenticated } = useAuth0();

  return isAuthenticated ? component : <Navigate to="/react-todo/" />;
};

export default ProtectedRoute; */